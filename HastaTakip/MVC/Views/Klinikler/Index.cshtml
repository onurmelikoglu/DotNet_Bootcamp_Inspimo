@* 1. yöntem *@
@* @model IEnumerable<Business.Models.KlinikModel> *@

@* 2. yöntem *@
@* @model List<KlinikModel> *@
@* 3. yöntem *@
@model IEnumerable<KlinikModel>

@*Generated from Custom Template.*@
@*Model namespace using directive should be added to _ViewImports.cshtml.*@

@{
    ViewData["Title"] = "Klinikler";
}

<div class="container">
    <h1>@ViewData["Title"]</h1>

    @* Details aksiyonuna yönlendirildiği için artık burada kulanmıyoruz*@
   @*  @if (TempData["Mesaj"] != null)
    {
        <p class="text-danger">@TempData["Mesaj"]</p>
    } *@

    <p>
        @* 1) Html: *@
        @* <a href="/Klinikler/Create">Yeni Klinik</a> *@
        @* 2) Html Helper: *@
        @* 1. yöntem *@
        @* @Html.ActionLink("Yeni Klinik", "Create", "Klinikler") *@
        @* 2. yöntem *@
        @* @Html.ActionLink("Yeni Klinik", "Create") *@

        @* 3) Tag helper *@
        @* 1. yöntem *@
        @* <a asp-action="Create" asp-controller="Klinikler">Yeni Klinik</a> *@
        @* 2. yöntem *@
        <a asp-action="Create">Yeni Klinik</a>
    </p>
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>
                    @* 1) Html: *@
                    @* Adı *@
                    @* 2) Html Helper: *@
                    @Html.DisplayNameFor(model => model.Adi) @* model özellik adı *@
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DoktorSayisiOutput)
                </th>
                @* <th>
                @Html.DisplayNameFor(model => model.Aciklamasi)
                </th> *@
                @*  <th>
                @Html.DisplayNameFor(model => model.Guid)
                </th> *@
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="w-50">
                        @* 1. yöntem *@
                        @* @item.Adi *@
                        @* 2. yöntem *@
                        @Html.DisplayFor(modelItem => item.Adi) @* model özellik verisi*@
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DoktorSayisiOutput)
                    </td>
                    @* <td>
                @Html.DisplayFor(modelItem => item.Aciklamasi)
                </td>
                <td>
                @Html.DisplayFor(modelItem => item.Guid)
                </td> *@
                    <td class="">
                        @* 1) Html: *@
                        @* <a href="/Klinikler/Details/@item.Id">Detay</a> *@
                        @* 2) Html Helper: *@
                        @* @Html.ActionLink("Detay", "Details", new { id = item.Id }) *@
                        @* 3) Tag Helper: *@
                        <a asp-action="GetItemJson" asp-route-id="@item.Id" target="_blank">Klinikler JSON</a>
                        <a asp-action="Details" asp-route-id="@item.Id">Detay</a>&nbsp;|&nbsp;

                        <a asp-action="Edit" asp-route-id="@item.Id">Düzenle</a>&nbsp;|&nbsp;
                        <a asp-action="Delete" asp-route-id="@item.Id">Sil</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

   
</div>





